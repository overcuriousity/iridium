// iridium-audit: append-only JSONL audit log with optional PGP signing.
// Phase 5 will implement Log::open / Log::append / Log::seal.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(true);
    }
}
