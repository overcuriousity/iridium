// iridium-ewf-sys: raw FFI bindings for libewf.
// Phase 1 will add build.rs + bindgen-generated bindings.
// This stub keeps the workspace compiling until then.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(true);
    }
}
