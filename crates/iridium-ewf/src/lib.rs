// iridium-ewf: safe Rust wrapper around libewf.
// Phase 4 will implement EwfHandle with open_write / write_chunk / close.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(true);
    }
}
